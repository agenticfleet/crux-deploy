# Support

Use GitHub Issues for reproducible compose, image, configuration, or deployment documentation problems.
Include `docker compose config`, service logs with secrets removed, host OS, Docker version, and selected `CRUX_VERSION`.
