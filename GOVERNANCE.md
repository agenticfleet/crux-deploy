# Governance

`crux-deploy` is maintained under the `agenticfleet` GitHub organization.

Changes to public ports, default image tags, persistent volumes, data stores, upgrade paths, or required secrets require documentation updates in the same change.
